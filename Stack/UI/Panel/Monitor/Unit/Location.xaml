<UserControl x:Class="Stack.UI.Panel.Monitor.Unit.Location"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Stack.UI.Panel.Monitor.Unit"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800" Width="100" Height="100">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        <TextBox Margin="10"
                 Text="{Binding X, RelativeSource={RelativeSource FindAncestor, AncestorType=UserControl}, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                 TextChanged="XAttributeChanged" />
        <TextBox Grid.Column="1" Margin="10" 
                 Text="{Binding Y, RelativeSource={RelativeSource FindAncestor, AncestorType=UserControl}, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" 
                 TextChanged="YAttributeChanged" />
    </Grid>
</UserControl>
